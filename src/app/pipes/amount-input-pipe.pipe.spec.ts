import { AmountInputPipePipe } from './amount-input-pipe.pipe';

describe('AmountInputPipePipe', () => {
  it('create an instance', () => {
    const pipe = new AmountInputPipePipe();
    expect(pipe).toBeTruthy();
  });
});
